# ============================================================
# HybridStyleNet — requirements.txt
# Target: Kaggle P100 (16 GB), CUDA 11.x
#
# Pre-installati su Kaggle (non serve specificare):
#   torch, torchvision, torchaudio  (>=2.0, con CUDA)
#   numpy, scipy, pandas
#   Pillow, matplotlib
#   scikit-learn
#   tqdm, pyyaml
#
# Tutto il resto va installato esplicitamente.
# ============================================================

# ── Core — richiesti per il funzionamento base ──────────────

# DINOv2 via torch.hub dipende da timm
timm>=0.9.0

# FAISS — retrieval approssimato IVF-PQ
# Su Kaggle P100 usare faiss-gpu per sfruttare la GPU
#faiss-gpu>=1.7.4

# ── Metriche di evaluation ──────────────────────────────────

# LPIPS — perceptual distance
lpips>=0.1.4

# SSIM differenziabile
pytorch-msssim>=0.2.1

# ── Logging ─────────────────────────────────────────────────

# Tensorboard (già presente su Kaggle, ma versione potrebbe essere vecchia)
#tensorboard>=2.12.0

# wandb — opzionale, commentare se non si usa
# wandb>=0.15.0

# ── I/O immagini ─────────────────────────────────────────────

# rawpy — solo per file RAW (ARW, DNG, CR2 ...)
# Non necessario se si usano PNG/TIFF già convertiti
# rawpy>=0.18.0

# ── Dipendenze implicite di DINOv2 torch.hub ────────────────
# (torch.hub.load le installa automaticamente, ma esplicitarle
#  evita conflitti di versione)
einops>=0.6.1
omegaconf>=2.3.0
